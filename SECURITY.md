# Security

Do not report security-sensitive defects in public issues.

For local deployments, treat checkpoint files and runtime logs as untrusted input when they originate outside the training environment.

The project does not require network access during training.
